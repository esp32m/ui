import { Lan } from '@mui/icons-material';
import { IContentPlugin } from '../app/types';

export const Network: IContentPlugin = {
  name: 'network',
  content: { title: 'Network', icon: Lan },
};